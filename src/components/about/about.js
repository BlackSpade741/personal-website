import React from 'react';

import './about.css';

const about = props => {
    return (
        <div id="about">
            this is the about section
        </div>
    );
}

export default about;