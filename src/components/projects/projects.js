import React from 'react';

import "./projects.css";

const projects = props => {
    return (
        <div id="projects">
            this is the projects section
        </div>
    );
}

export default projects;